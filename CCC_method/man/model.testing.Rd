\name{model.testing}
\alias{model.testing}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{A wrapper function to test a specific model on the X-Y dataset generated by the dataset.building function
%%  ~~function to do ... ~~
}
\description{tests whether a specific statistical model is able to predict the the Y variables of the X-Y dataset build by the function dataset.building, and if the model is applicable in the CCC_method
%%  ~~ A concise (1-5 lines) description of what the function does. ~~
}
\usage{
}
%- maybe also 'usage' for other objects documented here.
\arguments{model.testing(X1Y  <- the dataset given by the function dataset.building
ntest <- the number of training-test set splitting of the dataset to test with
ny <- the Y variable to be tested
model <- the statistical model to be tested among the 5 available: Lasso, ridge, logistic, penalized pls, plsr)
errori <- the type of error to use to test the models. To choose between "ppms" "Cees", "nerr"
per.test <- If TRUE, permutation is performed on the Y variable. default is FALSE
  \item{x}{
%%     ~~Describe \code{x} here~~
}
}
\details{The logistic model cannot be applied to the "phenolics" and "aliph" Y variables (3 and 7 respectively), because they are not binomial.
%%  ~~ If necessary, more details than the description above ~~
}
\value{a data.frame containing the mean accuracy of the model, its standard deviation and the RMSE)
%%  ~Describe the value returned
%%  If it is a LIST, use
%%  \item{comp1 }{Description of 'comp1'}
%%  \item{comp2 }{Description of 'comp2'}
%% ...
}
\references{
%% ~put references to the literature/web site here ~
}
\author{Luca Narduzzi nardluca@gmail.com
%%  ~~who you are~~
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{
dataset <- dataset.building(STD_RP)
model.test <- model.testing(dataset, ntest = 1000, ny = 1, model = "plsr", errori = "ppms", per.test = FALSE)
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ ~kwd1 }
\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
